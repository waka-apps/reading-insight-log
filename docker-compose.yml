services:
  dynamodb-local:
    image: amazon/dynamodb-local:latest
    container_name: reading-insight-dynamodb-local
    command: "-jar DynamoDBLocal.jar -sharedDb -dbPath /home/dynamodblocal/data"
    ports:
      - "8000:8000"
    volumes:
      - ./tmp/dynamodb:/home/dynamodblocal/data
